package org.plan.research.minimization.core.model

/**
 * Interface for any representation for some code modification for the base version.
 *
 * Base version is a version of a project that doesn't contain the target property (compiler error fault).
 * Usually, the base version is just an empty project.
 */
interface DDItem
